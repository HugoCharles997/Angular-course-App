import { Component, OnInit } from '@angular/core';              //import ES6 core


@Component({                                          //TypeScript
  selector: 'app-root',                               // custom html tag // component: bloc of code wich control part of the screen (chat, navbar, etc....)
  templateUrl: 'app.component.html'

})
export class AppComponent {         //pilots the logic of component //import OnInit interface l1 and implements it l7
}
